apiVersion: v1
kind: Service
metadata:
  name: {{ include "ajaychart.fullname" . }}
  labels:
    {{- include "ajaychart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ajaychart.selectorLabels" . | nindent 4 }}
