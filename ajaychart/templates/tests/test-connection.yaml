apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ajaychart.fullname" . }}-test-connection"
  labels:
    {{- include "ajaychart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ajaychart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
